<Page
    x:Class="Worktile.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Worktile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Worktile.Models"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="{x:Bind BgImage, Mode=OneWay}" Stretch="UniformToFill"/>
        </Grid.Background>

        <NavigationView x:Name="Nav"
                        PaneDisplayMode="LeftCompact"
                        MenuItemsSource="{x:Bind Apps}"
                        SelectedItem="{x:Bind SelectedApp, Mode=TwoWay}"
                        IsSettingsVisible="False"
                        BackRequested="Nav_BackRequested">
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="models:WtApp">
                    <NavigationViewItem Tag="{x:Bind Name}" Content="{x:Bind DisplayName}">
                        <NavigationViewItem.Icon>
                            <FontIcon Style="{ThemeResource WtfIcon}" Glyph="{x:Bind Glyph}"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>

            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox QueryIcon="Find"/>
            </NavigationView.AutoSuggestBox>

            <NavigationView.PaneHeader>
                <Image Source="{x:Bind Logo, Mode=OneWay}" HorizontalAlignment="Left" Margin="8 0 0 0"/>
            </NavigationView.PaneHeader>

            <NavigationView.PaneFooter>
                <StackPanel>
                    <!--<NavigationViewItem Content="{x:Bind DisplayName, Mode=OneWay}" Tapped="Me_Tapped">
                        <NavigationViewItem.Icon>
                            <FontIcon Style="{ThemeResource WtfIcon}" Glyph="&#xE642;"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>-->
                    <NavigationViewItem Content="{x:Bind DisplayName, Mode=OneWay}" Icon="Contact" Tapped="Me_Tapped"/>
                    <NavigationViewItem Content="设置" Icon="Setting" Tapped="Setting_Tapped"/>
                </StackPanel>
            </NavigationView.PaneFooter>

            <Frame x:Name="ContentFrame" Navigated="ContentFrame_Navigated">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </NavigationView>
        <ProgressRing Width="80" Height="80" IsActive="{x:Bind IsActive, Mode=OneWay}"/>
    </Grid>
</Page>
