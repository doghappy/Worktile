<UserControl
    x:Class="Worktile.Controls.ImageViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Worktile.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cvt="using:Worktile.Converters"
    mc:Ignorable="d"
    RequestedTheme="Dark"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <cvt:WtBoxImageSourceConverter x:Key="WtBoxImageSourceConverter"/>
    </UserControl.Resources>

    <FlipView ItemsSource="{x:Bind Images}">
        <FlipView.ItemTemplate>
            <DataTemplate x:DataType="x:String">
                <Image Source="{x:Bind}"/>
            </DataTemplate>
        </FlipView.ItemTemplate>
    </FlipView>
</UserControl>
