<Page
    x:Class="Worktile.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Worktile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mission="using:Worktile.WtControls.Mission"
    xmlns:wtcontrols="using:Worktile.WtControls"
    xmlns:tethys="using:Worktile.Tethys"
    xmlns:im="using:Worktile.WtControls.IM"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

<StackPanel Padding="32" Orientation="Horizontal">
    <ListView ItemsSource="{x:Bind Fonts}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="FontFamily">
                <StackPanel Orientation="Horizontal">
                    <PersonPicture DisplayName="Test" Initials="&#xE13D;" FontFamily="Segoe MDL2 Assets"/>
                    <TextBlock Text="&#xE13D;" FontFamily="{x:Bind}"/>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

    <ListView ItemsSource="{x:Bind Fonts}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="FontFamily">
                <StackPanel Orientation="Horizontal">
                    <PersonPicture DisplayName="Test" Initials="&#xE13D;" FontFamily="{x:Bind}"/>
                    <TextBlock Text="&#xE13D;" FontFamily="{x:Bind}"/>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</StackPanel>
</Page>
