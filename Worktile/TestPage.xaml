<Page
    x:Class="Worktile.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Worktile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mission="using:Worktile.WtControls.Mission"
    xmlns:tethys="using:Worktile.Tethys"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:Worktile.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Padding="32">
        <controls:MemberPicker/>

        <winui:TreeView ItemsSource="{x:Bind SampleDatas}" SelectionMode="Multiple">
            <winui:TreeView.ItemTemplate>
                <DataTemplate x:DataType="local:SampleData">
                    <winui:TreeViewItem ItemsSource="{x:Bind Chidren}"
                                Content="{x:Bind Name}"
                                IsExpanded="True"
                                IsSelected="{x:Bind IsChecked}"/>
                </DataTemplate>
            </winui:TreeView.ItemTemplate>
        </winui:TreeView>
    </StackPanel>
</Page>
