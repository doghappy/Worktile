<Page
    x:Class="Worktile.Windows.Main.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Worktile.Windows.Main"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Worktile.Windows.Main.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <NavigationView
            MenuItemsSource="{x:Bind ViewModel.Apps}">
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="models:WtApp">
                    <NavigationViewItem Content="{x:Bind DisplayName}" Tag="{x:Bind Name}">
                        <NavigationViewItem.Icon>
                            <FontIcon Style="{StaticResource WtfIcon}" Glyph="{x:Bind Icon}"/>
                        </NavigationViewItem.Icon>
                    </NavigationViewItem>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>
        </NavigationView>
    </Grid>
</Page>
